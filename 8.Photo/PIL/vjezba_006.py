from PIL import Image

fotografija = Image.open("8.Photo/Fotografije/Algebra_campus.jpg")

# fotografija.transpose(Image.FLIP_TOP_BOTTOM).show()
# fotografija.transpose(Image.FLIP_LEFT_RIGHT).show()
# fotografija.transpose(Image.ROTATE_90).show()
# fotografija.transpose(Image.ROTATE_180).show()
# fotografija.transpose(Image.ROTATE_270).show()
# fotografija.transpose(Image.TRANSPOSE).show()
# fotografija.transpose(Image.TRANSVERSE).show()





